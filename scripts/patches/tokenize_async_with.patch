--- fissix/pgen2/tokenize.py
+++ fissix/pgen2/tokenize.py
@@ -593,7 +593,7 @@
                         stashed = tok
                         continue
 
-                    if token in ("def", "for"):
+                    if token in ("def", "for", "with"):
                         if (
                             stashed
                             and stashed[0] == NAME
